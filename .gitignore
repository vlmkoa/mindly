node_modules
# Keep environment variables out of version control
.env
.env.local

# Python
__pycache__/
*.pyc
.venv/

# Local databases (data now lives in the Docker volume)
*.db
*.db-journal

# Machine-specific TLS roots (see backend/certs/README.md)
backend/certs/*.crt

.next/
*.pdf
.claude/

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Private launch notes + personal study guides — kept local only
LAUNCH_PLAN.md
AWS_STUDY.md
LEARNING_PATH.md
SECURITY_NOTES.md
RESUME_ITEM.md
REACT_TECHNIQUES.md

# Local scratch (screenshot drivers, captures)
tmp/

# Production secrets — only the .example template is committed
deploy/secrets.env
